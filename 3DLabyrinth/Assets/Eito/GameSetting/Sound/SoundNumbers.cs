using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SoundNumbers : MonoBehaviour
{
    //Slider masterSlider;
    //Text soundText;
    // Start is called before the first frame update
    void Start()
    {
        //float nowSound = 100f;
        //// Sliderコンポーネントを取得します
        //Slider mastorSlider = GetComponent<Slider>();
        //soundText = transform.Find("SoundText").GetComponent<Text>();
    }

    // Update is called once per frame
    void Update()
    {
       // masterSlider.value = 
    }
}
